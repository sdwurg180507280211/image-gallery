# 公众号样式库：共享首页模式

公众号样式库与“人物图片 / 医药KV”共享同一个 `index.html` 与同一组一级 Tab。

- 一级入口：人物图片 / 医药KV / 公众号
- 点击“公众号”仅切换当前首页内容区域，不跳转到独立页面
- 公众号组件继续复用 `wechat/index.html` 中的组件数据
- 每个组件只展示样式编号、预览和“复制 HTML”
- `wechat/` 目录作为组件数据与兼容入口保留，不作为主导航页面

此文件同时用于触发 GitHub Pages 重新构建，以发布共享首页版本。
