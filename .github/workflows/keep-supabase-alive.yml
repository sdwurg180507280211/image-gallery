name: Keep Supabase Alive

on:
  schedule:
    - cron: '17 3 */5 * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping dislike sync endpoint
        run: curl --fail --silent --show-error --retry 2 https://xkuzzmqtboclgvkvdlwd.supabase.co/functions/v1/image-dislikes > /dev/null
