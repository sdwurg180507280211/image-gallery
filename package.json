{
  "name": "visual-asset-library",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "A focused static visual asset library for character artwork and medical KV backgrounds.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "serve": "python3 -m http.server 4173 --directory dist",
    "delete-assets": "node scripts/delete-assets.mjs",
    "prepare-r2-shadow": "node scripts/prepare-r2-shadow.mjs"
  },
  "dependencies": {
    "sharp": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
